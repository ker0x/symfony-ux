{# @prop variant 'default'|'secondary'|'destructive'|'outline'|'ghost'|'link' The button style variant. #}
{# @prop size 'default'|'xs'|'sm'|'lg'|'icon'|'icon-xs'|'icon-sm'|'icon-lg' The button size. #}
{# @block content The button label. #}
{%- props variant = 'outline', size = 'default' -%}
<twig:Button
    variant="{{ variant }}"
    size="{{ size }}"
    data-slot="questionnaire-skip"
    data-questionnaire-target="skip"
    data-action="click->questionnaire#skip"
    class="col-start-2 row-start-1 min-h-11 justify-self-end sm:min-h-0 [&[hidden]]:hidden"
    {{ ...attributes }}
>
    {{- block(outerBlocks.content)|default('Skip') -}}
</twig:Button>
